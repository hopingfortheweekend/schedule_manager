# 轻简日程管理 — 产品需求文档 (PRD)

> **文档状态：** 草稿
> **版本：** v0.2
> **作者：** Jasmine
> **日期：** 2026-06-09

---

## 1. 背景与目标

<!-- 引导：用 3-5 句话回答 ——
  这个项目最初是为了解决你自己的什么痛点？
  目前 v0.1 已经做到了什么？
  这一版（v0.2）想达到什么效果？
-->

这个项目缘起我个人的时间管理需要。因为我目前没有在市面上看到一款功能简单明了、使用方便、界面干净、功能可自定义的轻量化日程管理软件，又有记录日常学习、工作任务的需要。

目前的版本（v0.1）已经可以分类记录短期的每日任务与长期项目，其中长期项目可以添加具体步骤并设置截止时间；可以在日历界面根据已有日程的截止日期、以不同的颜色区分显示其紧迫程度。每个日程的已/未完成状态、截止时间和日程的具体内容可以自定义、随时修改。

新一版（v0.2）准备完成以下更新。一是添加一个数据统计看板。看板可以总结整理月度/季度的日程完成情况，将其做成趋势图、并计算日程的完成率。二是明确用户写入数据的存储位置，可以将这些数据输出为csv/excel格式的文件、便于保存。三是增加删除日程的功能。四是将长期项目中的截至日期为当日的步骤同样显示在当日要完成的页面，一并提醒用户。

---

## 2. 目标用户

<!-- 引导：描述 1-2 类典型用户。
  可以是你自己，也可以是假想的其他用户。
  格式示例：
    - 用户画像 A：大学生/职场新人，拖延倾向，需要一个简单工具可视化 DDL
    - 核心场景：每天早上打开看一眼"今天有什么截止"、"接下来几天还有什么"
-->

用户画像：大学生/职场新人，拖延倾向、记性不太好，需要一个简单工具可视化日常活动和想做的事情以更好地规划生活
核心场景：每天开始学习/工作时打开软件看一眼或者设置下今天或之后几天要做什么；遇到日程（如一个作业/跟朋友团建）随手加到日程记录里

---

## 3. 用户故事

<!-- 引导：用 "作为____，我希望____，以便____" 的句式。
  写 5-8 条，覆盖核心功能。不用想太复杂，从你自己真实的使用场景出发就行。
  示例：
    - 作为拖延症用户，我希望在日历上看到所有 DDL 的颜色标记，以便一眼判断哪些任务紧急
-->

| 编号 | 用户故事 | 优先级 |
|------|----------|--------|
| US-01 | 作为健忘者，我希望在日历界面清晰看到我什么时候有任务/活动 DDL 以及它们距离今天有多久，以便提前安排工作学习，为重要事项留出准备时间 | Must |
| US-02 | 作为拖延症，我希望打开软件就能看到当日要做什么，且添加和编辑的操作足够流畅，以便不给自己犹豫和拖延的借口 | Must |
| US-03 | 作为需要回顾效率的用户，我希望看到月度/季度的任务完成统计，以便了解自己的时间利用情况、发现规律和改进空间 | Must |
| US-04 | 作为同时处理多个项目的人，我希望长期项目中截止日期为当日的步骤能自动出现在"每日任务"页面，以免遗漏分散在不同项目里的当日待办 | Must |
| US-05 | 作为会犯错的人，我希望可以删除不再需要的日程或项目，以便保持列表整洁、不被过时信息干扰 | Must |
| US-06 | 作为有存档需求的用户，我希望将数据导出为 CSV/Excel 文件，以便在软件之外也能查看和备份 | Should |
| US-07 | 作为审美较佳者，我希望软件界面能好看一些，当前版本虽然简洁但视觉上比较老旧 | Could |

---

## 4. 功能范围（MoSCoW 优先级）

<!-- 引导：把你所有想做和可能做的功能列出来，分到四个桶里。
  - Must have：没有它这版就不算完成
  - Should have：重要但可以下一版补
  - Could have：锦上添花
  - Won't have（this version）：明确这版不做，避免范围蔓延
-->

| 优先级 | 功能 | 对应故事 | 说明 |
|--------|------|----------|------|
| Must | 日程删除 | US-05 | 支持删除任务和项目步骤，含确认弹窗防止误删 |
| Must | 月度/季度统计看板 | US-03 | 折线图展示完成 vs 全部的趋势，支持切换时间范围 |
| Must | 当日截止步骤汇总 | US-04 | 长期项目中截止日期为当日的步骤，自动显示在"每日任务"页面 |
| Should | 数据导出（CSV/Excel） | US-06 | 支持按时间范围筛选并导出为文件，使用简化的日期选择方式 |
| Could | 前端视觉优化 | US-08 | 改进配色、字体、布局，提升整体美感 |
| Won't | 未完成原因记录 | — | 文字描述与统计图难以融合，暂时不做 |
| Won't | 多用户/云端同步 | — | 保持单用户本地存储的定位 |
| Won't | 移动端适配 | — | 当前仅支持 PC 端（Windows/macOS/Linux） |

---

## 5. 功能详述

<!-- 引导：对每个 Must 和 Should 功能，展开描述具体的交互流程。
  不需要写代码逻辑，重点是"用户看到什么、做什么、系统怎么响应"。
  格式建议：一个功能 = 一段描述 + 关键交互步骤 + 边界情况
-->

### 5.1 日程删除

- **交互流程：** 用户双击日程出现日程具体内容的页面时，目前只有修改日程内容、确认和取消的互动内容。可以在这个页面中，将修改列为一栏，用户点击修改的互动框并且确实修改了内容时，再出现一个页面询问用户是否确认修改，待用户确认后保存修改。同时，修改任务内容的栏目下面增加一个删除日程的选项。当用户点击这个选项时，可以再出现一个确认是否删除日程的提醒页面，用户确认后再删除日程。
- **边界情况：** 暂无。

### 5.2 数据统计看板

- **交互流程：** 
  目前上方工具栏有每日任务、长期项目、日历视图三个功能。可以将“数据统计”放在日历视图之后。
  用户点击进入该功能后，可以看到左侧有一个可以互动切换时间范围大小的折线图。图的纵坐标对应日程数量、横坐标对应时间（节点为周或月）。图中每个横坐标对应着“完成”与“全部”两个指标，中间所差的纵坐标距离就是用户未（按时）完成的日程数量，两个指标与折线会采用不同的颜色标识。每个节点会在旁边用小字显示具体日程数量。
  进入该页面时的折线图默认显示用户过去4周（1个月时间）的两道折线，时间范围可以切换为过去n周或n月。切换时间范围的按钮就在折线图下方的图名当中，图名会显示这是“过去几周/几月或未来几周/几月或过去几周/几月到未来几周/几月（后括号内附上具体的日期说明）的数据统计图”，时间的位置是可以点击互动的。点击时间后会出现一个日历界面，用来确认时间范围。
- **边界情况：** 如果用户选择过多的时间（好多年以前/以后）？目前最多支持过去到未来1年范围的日程调用。

### 5.3 当日日程汇总

- **交互流程：** 目前每日任务的页面可添加和显示当日的任务。新一版的改动希望用户在添加长期项目之步骤时，若有步骤的截止日期为当日，则一并显示在每日任务的页面，并且可以在该栏当中用加粗字体备注显示该任务属于某个长期项目。此时每日任务的排列顺序按创建该日程的时间（若是长期项目的步骤则以该步骤的创建时间为准）倒序排列，也即最新创建的日程在最上面。
- **边界情况：** 暂无。

### 5.4 数据导出

- **交互流程：** 
  在标签栏右侧增加一个"导出数据"按钮。点击后弹出窗口，用户通过两个日期输入框（起始日期 + 结束日期）选择导出范围，另有快捷按钮"本月""近3个月""全部"。确认后弹出文件保存对话框，用户选择保存位置和文件名。导出格式为 CSV，包含：任务/步骤名称、所属项目（如有）、截止日期、完成状态。
- **边界情况：** 若选中的时间范围内无数据，提示用户"所选范围无日程记录"；日期范围为过去/未来 1 年内。

---

## 6. 验收标准

<!-- 引导：用可测试的语言描述"怎么样算做完了"。
  每条标准应该是 YES/NO 能判断的。标准需要包含数据对不对的判断。
  示例："用户双击任务后弹出编辑窗口，预填当前内容，修改后成功保存"
-->

- [ ] 标准 1：用户点击删除日程的按钮，在确认窗口中确认删除，之后日程被删除
- [ ] 标准 2：用户点击“数据统计”后进入统计图视图
- [ ] 标准 3：用户点击折线图名中的时间会进入日历视图
- [ ] 标准 4：用户可根据日期或周、月来选择折线图的时间范围，范围修改后，折线图的内容同样变更
- [ ] 标准 5：用户点击导出数据的按钮后，出现日历视图
- [ ] 标准 6：用户可根据日期或周、月来选择数据导出的时间范围，范围修改并确认后，导出的csv/excel文件出现在其选择的文件位置

---

## 7. 非功能需求

<!-- 引导：考虑性能、数据安全、兼容性等。
  这个项目很小，写 2-3 条就行。
  示例："数据文件损坏时，程序应给出明确提示而不是崩溃"
-->

数据文件损坏时，程序应给出明确提示而不是崩溃；
产品目前只支持PC端，前面的设计也都是根据键鼠操作来展开的。

---

## 8. 风险与假设

<!-- 引导：有哪些不确定的事情？做了哪些假设？
  示例："假设用户只有一个人使用，暂不考虑多用户/数据共享"
-->

目前给软件设置的支持数据时间是过去到未来1年的范围，超出这个时间范围的还没有想好怎么处理；
目前是一个用户在用，如果以后考虑给多人使用，他们的数据放哪里也是个问题。
